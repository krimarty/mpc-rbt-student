name: "communication_example"
root: <%= Dir.pwd %>

startup_window: monitoring
windows:
  - monitoring:
      layout: even-vertical
      panes:
        - watch -n 1 "systemctl status example_receiver.service"
        - watch -n 1 "systemctl status example_sender.service"
